const fetch = require('node-fetch'); async function getExistingSchoolId() { try { const response = await fetch('http://localhost:5000/api/Events/682f0b4b324f105f376b617f'); const events = await response.json(); if (events && events.length > 0) { console.log('Found school ID:', events[0].school); return events[0].school; } return null; } catch (error) { console.error('Error fetching school ID:', error); return null; } } getExistingSchoolId();
